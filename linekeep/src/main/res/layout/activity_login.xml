<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    tools:context="cn.linekeep.pangu.vcan.ui.LoginActivity">

    <ImageView
        android:id="@+id/logo_linekeep"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:src="@drawable/logo" />

    <EditText
        android:textSize="14sp"
        android:hint="请输入手机号/用户名"
        android:id="@+id/et_username"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/logo_linekeep"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="26dp" />

    <EditText
        android:textSize="14sp"
        android:hint="请输入密码"
        android:id="@+id/et_password"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_username"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="26dp" />

    <TextView
        android:id="@+id/tv_other_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@id/et_password"
        android:layout_below="@+id/et_password"
        android:text="第三方登录"
        android:textSize="12sp" />

    <Button
        android:onClick="Login"
        android:layout_marginTop="50dp"
        android:id="@+id/btn_login"
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:layout_below="@id/tv_other_login"
        android:layout_centerHorizontal="true"
        android:background="#32608A"
        android:text="登录" />

    <TextView
        android:onClick="onRegisterClick"
        android:layout_marginLeft="10dp"
        android:layout_marginBottom="5dp"
        android:layout_alignParentBottom="true"
        android:id="@+id/tv_regist"
        android:textSize="12sp"
        android:text="注册"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:onClick="forget"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="5dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="忘记密码"
        android:id="@+id/tv_forget_passward"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>
